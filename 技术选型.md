# 2026 Ball Game 技术选型文档

## 1. 文档目的

本文确定项目第一阶段的客户端、平台接入、后端、数据库、资源管理和部署方案，作为后续原型开发、UI 外包和任务拆分的统一依据。

当前发布顺序：

1. 微信小游戏首发
2. 抖音小游戏适配
3. 暂不考虑 TikTok

## 2. 产品范围

### 2.1 第一版

第一版只实现单机玩法和平台分享：

- 蓄力或点击跳跃
- 无限生成、下落或移动的平台
- 层数和分数计算
- 每 100 层触发一次“三选一”强化
- 失败、结算和激励广告复活
- 角色和皮肤展示、解锁或购买
- 微信/抖音平台分享
- 本地存档
- 广告、支付和必要的平台登录能力

第一版不实现：

- 实时联机
- 匹配竞争
- 好友竞争
- 排行榜
- 对局画面同步
- 聊天、房间和公会

这里的“单机”指玩法不依赖其他玩家，不代表完全不需要服务器。广告可以主要依赖平台客户端 SDK，但只要出售付费皮肤或角色，就仍需要后端处理订单回调、验签、幂等发货和商品权益。

### 2.2 第二版或第三版

后续考虑“影子竞赛”，不做完整实时同步：

- 对手显示为蓝色小球，不显示对方的完整角色和真实动作。
- 每跳 10 层或 20 层上传一次层数、分数、时间戳和少量状态。
- 本地根据最新检查点模拟蓝色小球跳跃。
- 网络延迟只影响对手影子的显示，不影响本地跳跃和判定。
- 服务端负责匹配、检查点转发、结果结算和基本反作弊。
- 好友挑战可通过分享链接或平台关系链发起。

这个方案属于低频检查点同步，不属于严格实时对战。未来优先使用 HTTPS 短请求或短轮询；只有验证确实需要更高实时性时才增加 WebSocket。

## 3. 总体技术结论

项目采用以下技术栈：

- 游戏引擎：Unity 2022.3.14f1 LTS
- 渲染管线：URP
- 开发语言：C#
- 构建基础：Unity WebGL、IL2CPP、WebAssembly
- 微信适配：微信小游戏 Unity/团结转换 SDK
- 抖音适配：抖音 TTSDK WebGL 方案
- 资源管理：Unity Addressables
- 后端：Node.js 24 LTS、TypeScript、NestJS
- 主数据库：PostgreSQL
- 缓存：Redis，第一版按实际功能启用
- 通信：HTTPS REST API
- 资源分发：对象存储和 CDN
- 后端部署：Docker
- API 文档：OpenAPI
- 监控：结构化日志、错误告警、OpenTelemetry
- 版本管理：Git

这套选型适合当前的轻度竖屏游戏。Unity、Node.js 和 PostgreSQL 可以继续使用，但必须把平台 SDK 与核心玩法隔离，不能让微信或抖音接口散落在游戏逻辑中。

## 4. 客户端选型

### 4.1 Unity 版本

固定使用 Unity 2022.3.14f1 LTS，不在开发中途随意升级。

选择原因：

- 位于微信小游戏官方已验证的 Unity 2022.3.1 至 2022.3.14 范围内。
- 符合抖音 WebGL 方案对 Unity 2022 LTS 的支持方向。
- 支持 ASTC、ETC2 压缩纹理。
- 相比旧版本具备更好的 WebGL 包体、编译和内存分析能力。
- 能够满足当前轻量 3D 跳跃玩法。

立项第一周必须用该版本分别生成微信和抖音最小构建，并在 Android、iOS 真机验证。只有验证失败且确认是引擎兼容问题时，才调整小版本。

暂不选择 Unity 6。当前两个小游戏平台的成熟转换链路主要围绕 Unity 2021/2022 LTS，直接选择更新引擎会增加 SDK 兼容和构建风险。

### 4.2 渲染方案

使用 URP，不使用 HDRP。

建议基线：

- 竖屏
- 目标帧率 30 FPS
- 简化光照和阴影
- 少量实时光源
- 控制透明材质和粒子数量
- 平台、特效和提示物使用对象池
- 低端设备降低分辨率、阴影和粒子质量
- 避免依赖后处理实现核心视觉效果

小游戏的 WebGL/Wasm 性能低于原生 App。项目从灰盒阶段就必须在真机测试，不能等美术资源全部完成后再优化。

### 4.3 代码架构

客户端划分为以下模块：

- `Game.Core`：跳跃、平台、摄像机、层数、分数、失败和复活。
- `Game.Modifiers`：三选一强化系统。
- `Game.Content`：角色、皮肤、强化卡和数值配置。
- `Game.Platform`：登录、广告、支付、分享和生命周期接口。
- `Game.Save`：本地存档和未来云存档接口。
- `Game.UI`：界面逻辑、页面导航和安全区适配。
- `Game.Telemetry`：埋点、日志和错误上报。

核心玩法不能直接调用 `WX`、`tt` 或具体平台 SDK。平台能力统一通过抽象接口访问，并提供：

- 微信实现
- 抖音实现
- Unity 编辑器模拟实现

这样可以在编辑器中调试玩法，也能在不修改核心玩法的前提下更换平台 SDK。

### 4.4 三选一强化系统

强化系统采用数据驱动，不为每张强化卡在主流程中编写条件分支。

每个强化至少包含：

- 唯一 ID
- 名称和说明
- 图标
- 品质或权重
- 是否允许重复
- 最大叠加次数
- 生效条件
- 一个或多个效果参数

效果使用可组合的 Modifier，例如：

- 平台坠落速度乘以 0.9
- 平台速度乘以 1.1
- 得分倍率乘以 1.15
- 增加一次复活次数
- 改变跳跃力度或容错范围

此结构便于持续增加强化内容，也方便后续做远程数值调整。

### 4.5 资源管理

使用 Addressables 管理角色、皮肤、特效、音频和后续场景。

原则：

- 首包只包含启动、主界面和第一局必需资源。
- 皮肤和角色资源按需下载。
- 不把全部资源放入 `Resources`。
- 资源包通过对象存储和 CDN 分发。
- 配置和资源都带版本号，避免客户端与资源版本不匹配。
- 单个资源包保持较小，避免一次下载和解压占用过多内存。
- 中文字体进行字符裁剪，避免完整字体进入首包。

## 5. 平台适配

### 5.1 微信小游戏

Unity WebGL 构建后，使用微信官方 Unity/团结小游戏转换 SDK 生成微信小游戏工程。

第一版接入：

- 生命周期
- 平台登录
- 分享
- 激励视频广告
- 必要时接入插屏广告
- 虚拟支付 2.0
- 支付服务端回调
- 网络域名和下载域名白名单

广告不能影响核心操作。激励广告适合用于复活、额外奖励或试用皮肤；插屏广告只能放在结算等自然停顿点。

### 5.2 抖音小游戏

使用 TTSDK 的 WebGL 新包体格式，不采用面向旧项目的 Native 方案。

第一阶段只保证项目架构可适配抖音；微信版本稳定后再完成：

- TTSDK 构建
- 登录
- 分享
- 广告
- 支付
- 生命周期
- 真机性能和包体优化

抖音实现必须位于独立程序集或独立目录中，避免影响微信构建。

## 6. 后端选型

### 6.1 Node.js 和 NestJS

使用：

- Node.js 24 LTS
- TypeScript
- NestJS
- REST API
- OpenAPI

第一版采用模块化单体，不使用微服务、Kubernetes 或复杂消息队列。

选择 NestJS 的原因：

- 模块边界清晰。
- 适合登录、订单、商品和用户权益等业务。
- 便于实现参数校验、鉴权、限流和统一异常处理。
- 后续增加排行榜或影子竞赛时不需要重写整个服务。

### 6.2 第一版后端职责

第一版服务端只承担必要功能：

- 微信和抖音登录凭证换取内部会话。
- 管理内部玩家 ID 与平台 OpenID 的映射。
- 保存商品、角色、皮肤和玩家权益。
- 创建支付订单。
- 接收平台支付回调。
- 验证支付签名。
- 以平台订单号保证幂等发货。
- 提供订单主动查询和补单能力。
- 下发可远程调整的商品和活动配置。
- 接收关键运营事件和错误日志。

本地单机进度第一版保存在客户端。若未来需要跨设备同步，再增加云存档，并明确存档冲突处理规则。

### 6.3 后续后端职责

排行榜、好友挑战和影子竞赛推迟到第二版或第三版：

- 匹配队列
- 比赛会话
- 每 10 层或 20 层的检查点
- 对手最新分数和进度
- 比赛结果
- 超时和断线处理
- 基础反作弊
- 好友挑战邀请

第一版不为这些功能提前建设复杂基础设施，只保留模块和协议扩展空间。

## 7. 数据库选型

### 7.1 PostgreSQL

“大象数据库”即 PostgreSQL，适合本项目，作为唯一主数据库。

第一版预计保存：

- 玩家
- 平台身份
- 内部会话
- 商品
- 角色和皮肤定义
- 玩家已拥有权益
- 支付订单
- 发货记录
- 配置版本
- 审计日志

支付订单号、平台订单号和发货记录必须建立唯一约束，不能只依赖业务代码防止重复发货。

ORM 使用 Prisma，并通过迁移文件管理数据库结构。生产环境禁止客户端直接连接 PostgreSQL。

### 7.2 Redis

第一版不强制依赖 Redis 才能启动。出现以下需求时启用：

- API 限流
- 短期会话缓存
- 支付回调幂等锁
- 高频配置缓存

第二版或第三版增加竞争功能后，Redis 可用于：

- 匹配队列
- 比赛临时状态
- 检查点缓存
- 排行榜 Sorted Set

PostgreSQL 仍保存最终结果，Redis 不作为唯一永久数据源。

## 8. 数据和网络原则

- 客户端与后端只通过 HTTPS 通信。
- 第一版不使用 WebSocket。
- 平台密钥、支付密钥和数据库密码只能保存在服务端。
- 支付结果以前端成功回调为提示，以服务端平台回调或主动查询为最终依据。
- 所有支付和奖励接口都必须支持重复请求。
- 客户端分数不能直接作为可信结果。
- 未来影子竞赛的蓝色小球只是表现层，胜负由服务端检查点和结果规则决定。

## 9. 部署方案

微信和抖音阶段使用中国大陆云服务：

- 一台应用服务或容器服务运行 NestJS。
- 托管 PostgreSQL。
- 按需要启用托管 Redis。
- 对象存储保存 Addressables 资源。
- CDN 分发游戏资源。
- 使用已备案的 HTTPS 域名。
- 区分开发、测试和生产环境。

初期不需要多区域部署和自动扩缩容。上线后根据并发、数据库连接数、接口延迟和广告活动流量决定是否扩容。

## 10. UI 外包技术要求

UI 外包交付应包含：

- Figma 源文件
- 字体和素材授权说明
- 页面状态和交互说明
- 可编辑源文件
- 图集和切图
- 九宫格区域说明
- 动效稿或动效参数
- 常规屏、长屏和异形屏适配示意

建议设计基准为竖屏 750 × 1334，并要求适配安全区。具体像素密度由 Unity Canvas Scaler 处理。

首批页面：

- 启动页
- 主界面
- 角色和皮肤页
- 局内 HUD
- 每 100 层三选一页面
- 暂停页
- 复活页
- 结算页
- 分享页
- 商店和购买确认页

外包负责视觉和规范，内部负责 Unity 页面搭建、数据绑定、平台接口和性能控制。

## 11. 测试和性能基线

第一周技术验证必须记录：

- 微信和抖音构建是否成功
- Android 和 iOS 真机是否可运行
- Wasm 体积
- 首包和总包体积
- 首屏可操作时间
- 峰值内存
- 平均帧率
- 长时间运行发热
- 切前后台是否恢复
- 广告关闭后音频和时间是否恢复
- 分享取消和失败是否正确处理

第一版目标：

- 中低端设备稳定 30 FPS。
- 连续运行时内存不持续增长。
- 游戏对象生成不造成明显卡顿。
- 弱网和断网不影响纯单机对局。
- 平台广告、支付或分享失败时，游戏能够恢复且不重复发奖。

## 12. 版本路线

### 第一版：单机验证

- 完整单机循环
- 数据驱动强化
- 本地存档
- 分享
- 激励广告
- 角色和皮肤
- 若开放真实付费，则同时上线最小支付后端

### 第二版：运营和轻社交

- 云存档
- 活动配置
- 排行榜或好友挑战
- 分享邀请
- 更完整的商品和皮肤运营

### 第三版：影子竞赛

- 匹配或好友约战
- 蓝色小球对手
- 每 10 层或 20 层同步检查点
- 服务端结果结算
- 断线、超时和反作弊

实际先选择每 20 层同步，降低请求量；测试发现对手反馈过慢后再调整为每 10 层。同步间隔应由远程配置控制，不写死在客户端。

## 13. 暂不采用的技术

第一版不采用：

- TikTok SDK
- 实时多人同步框架
- Photon、Mirror 或 Netcode for GameObjects
- WebSocket
- 微服务
- Kubernetes
- 消息队列
- 独立数据仓库
- Unity 6
- HDRP
- 客户端 SQLite

这些技术当前不能直接提高第一版成功率，反而会增加包体、适配、维护和测试成本。

## 14. 知识产权注意事项

“英雄联盟海克斯乱斗”只能作为内部玩法参考。未获得 Riot Games 授权时，项目名称、宣传文案、角色、美术、图标、音效和强化名称均不能使用英雄联盟资产或形成明显混淆。

对外应使用原创定位，例如“跳跃 + Roguelite 三选一强化”，并建立独立世界观、角色和视觉风格。

## 15. 最终决定

本项目第一阶段使用 Unity 2022.3.14f1 LTS、URP、C#、Addressables 开发客户端，以微信小游戏为首发目标，随后适配抖音小游戏。第一版玩法保持单机，只接分享、广告和必要的支付能力。

后端使用 Node.js 24 LTS、TypeScript、NestJS 和 PostgreSQL；Redis 作为按需组件，不在第一版制造强依赖。未来的好友或匹配竞争采用低频检查点同步的蓝色小球影子方案，不进行完整画面和物理状态同步。
